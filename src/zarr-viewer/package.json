{
  "name": "zarr-viewer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone OME-Zarr WebGPU volume viewer (extracted from Prism Demo 26).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@webgpu/types": "^0.1.52",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^3.2.7"
  }
}
